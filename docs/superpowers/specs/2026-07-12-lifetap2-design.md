# LifeTap 2 — Design Spec (2026-07-12)

Rebuild of the LifeTap MTG-style life counter as a Flutter app, feature-parity with the original plus first-class multi-touch. Approved in discussion 2026-07-12.

## Goals

1. Everything the original LifeTap does (baseline below), no regressions.
2. Headline improvement: true multi-touch — simultaneous, independent touch handling per player zone.
3. Real automated testing, run in Docker containers in CI.

## Baseline feature parity

- 2-player face-to-face split screen; tap to +/- life; starting-life presets 20/25/30/40/60; reset game.
- New games default to **4 players at 40 life** (2/3/5/6 also selectable in the new-game dialog; the starting-life set stays 20/25/30/40/60).
- 3–6 players with adaptive zone layout (zones rotated to face each seat).
- Commander damage per opponent (auto-applies life loss), poison/energy/experience counters.
- Utilities: dice roll, coin flip, first-player picker, life-change history + undo, keep-screen-awake, per-player colors/names.

## Multi-touch features (the improvement)

| Feature | Behavior |
|---|---|
| Simultaneous taps | Touches in different zones are fully independent — two+ players tapping at the same instant all register; nothing queued or dropped |
| Tap increment (±1) | A quick tap in a zone is always **±1**, regardless of how many fingers touch — the finger count no longer scales the amount. Sign is resolved in the zone's **own upright frame**: the down-point is rotated by −q·90° (q = the seat's quarter-turns) about the zone center, then **left-of-the-seated-player − , right +**. So every player's own-left is − and own-right is + regardless of how their seat faces (an upside-down top seat's screen-left is that player's right). The `PointerRouter` carries a `zoneTurns` list parallel to `zones`; hold-repeat and swipe use the same per-seat sign |
| Hold and swipe (±1 / ±10) | The only life-change amounts are **±1 and ±10**. A stationary hold (movement under the drag slop) auto-repeats **±1** after ~300ms and **accelerates** — the amount stays fixed at 1 forever while the interval between repeats shrinks (~300ms → ~50ms). A **horizontal swipe** (a drag past the swipe threshold along the player's own left/right axis, using the same per-seat `_signAt` transform) is a single **±10**, emitted once: toward the player's own left = −10, toward their own right = +10. Vertical drags do nothing. Precedence: a swipe wins (once past threshold it fires ±10 and suppresses the tap and any hold-repeat); otherwise a stationary hold repeats ±1; otherwise a quick release is a ±1 tap |
| Hold-together ritual | ≥1 finger held in **every** active zone for 1.5s (with progress ring) = start/reset game — replaces a reset button and prevents accidents |
| Multi-touch first-player picker | Everyone touches and holds anywhere; after the hold window a random finger wins — replaces the menu-based picker |

## Architecture

**Decision: raw `Listener` pointer events + custom per-zone pointer state machine, NOT `GestureDetector`.** Flutter's gesture arena globally disambiguates competing gestures, which serializes cross-zone gestures — exactly the failure this rebuild removes. Rejected alternatives: per-zone GestureDetectors (arena conflicts), Flame engine (overkill).

Three layers, dependencies point downward only:

```
ui/      Flutter widgets: zone grid, overlays, sheets, theming
touch/   pure Dart pointer state machine (no Flutter widget deps beyond event types)
game/    pure Dart domain: state, events, reducers
```

- `game/`: `GameState` (players: life, commanderDamage map, poison/energy/experience, name, color; settings: playerCount 2–6, startingLife 20/25/30/40/60). **Event-sourced**: every mutation is a `GameEvent` appended to history → undo and the history view are projections. State managed with Riverpod.
- `touch/`: `PointerRouter` claims each pointer for the zone it lands in; per-zone state machine classifies a quick release → ±1 tap, a stationary hold → accelerating ±1 repeat, a horizontal drag past threshold → a single ±10 swipe (so the only amounts are ±1 and ±10); a global recognizer watches for the hold-together ritual and the first-player picker. Pure Dart: unit-testable with synthesized pointer streams, no emulator needed.
- `ui/`: zone layouts for 2 (mirrored halves), 3 (1+2), 4 (quadrants), 5–6 (grid); horizontal swipe switches a zone's counter mode (life ⇄ poison/energy/exp); commander-damage overlay per player; dice/coin; history sheet with undo; wakelock; color/name editor. Each zone shows an editable player-name label pinned to its top edge, rotated to the seat's facing so it reads right-side-up; the label sits above the pointer `Listener` so its own hit area is consumed (tapping it opens the editor, not a life change) while the rest of the zone still taps life. Tapping it opens a **seat-rotated** rename editor: the live name preview, the autofocused prefilled `TextField`, and the Cancel/Done buttons are wrapped in a `RotatedBox` of that seat's `quarterTurns` (from `seatQuarterTurns(playerCount)`), so a side-facing player types into a field that faces them, matching how their life number reads. The **system keyboard stays upright** — that is an OS constraint we don't fight; instead the panel is pinned to the top of the screen and lifted above the keyboard inset (`MediaQuery.viewInsets.bottom`), with a `FittedBox` scaling the sideways panel down to whatever height is left, so the field stays visible and reachable for all four seat rotations. Done/submit dispatches `RenamePlayer` (empty/whitespace ignored); an outside tap or Cancel dismisses without change. The gear settings sheet remains a second path to the same rename.

Because the OS keyboard can't be rotated to face a side seat, an **optional in-app on-screen keyboard** (default **on**; toggle "In-app keyboard" under Settings → Gameplay) replaces it in the rename editor. When on, the editor drops the editable `TextField` (which would summon the OS keyboard) for a non-editable bordered display box with a trailing caret plus the live preview, and renders a compact QWERTY (`_InAppKeyboard`) **inside the same `RotatedBox`** as the field, so keys, field, and preview all rotate to the seat together; with no OS keyboard the rotated panel is centered rather than top-pinned. Shift starts on to capitalize the first letter and auto-unshifts after each letter; the return key and Done both submit; Cancel aborts. Flipping the toggle off restores the exact native-keyboard behavior above. The keyboard is built from our own Material widgets only (no third-party packages).

## Visual design (2026-07-12 reskin)

An independent black + cyan reskin following a common MTG life-counter layout. It restyles `ui/` and the theme only; the pure `game/` and `touch/` layers and their behavior are untouched. All iconography is our own (Material icons / painted shapes); backgrounds still come from the existing Scryfall-art-by-card-name mechanism — no third-party art or icon assets are embedded.

**Design tokens** (`lib/ui/theme.dart`, `LifeTapColors`): pure-black `#000000` background; cyan accent `#33C7F0` for selected states, toggles-on, the player-count badge ring, and active chips; surfaces/sheets `#141414`, chips `#1E1E1E`, unselected chip `#2A2A2A`, hairline dividers `#2C2C2C`; white primary / `#9E9E9E` secondary text; positive delta green `#47C266`, negative red `#E5533C`, poison purple `#9B6DE8`. `buildLifeTapTheme()` is a dark Material 3 theme with the black scaffold and cyan `colorScheme.primary`. Landscape orientation stays locked in `main.dart`.

**Player zone** — either full-bleed commander art with a dark scrim, or (default, no art) a **near-black** `#0D0D0D` fill that uses the player's color only as an accent; everything seat-rotated via `seatQuarterTurns`:
- Empty (no-art) zones render near-black with the player's color as a **3px border accent** and a **name pill tinted with the player's color** (color at ~30% over translucent black); the big life number stays white. A fresh 4-player game therefore reads dark with subtle per-player accents rather than four colored blocks. When art is present the zone keeps the neutral divider hairline and the art + scrim path is unchanged.
- A huge white life number, `w800`, sized ≈ shortest-zone-side × 0.42 via `LayoutBuilder` + `FittedBox` so it never overflows, with a soft black shadow for legibility over art.
- A rounded name pill (radius 18) near the top-inner edge, tinted with the player's color; tapping it opens the **seat-rotated** rename editor (the field/preview/buttons rotate to the seat's `quarterTurns`; the keyboard stays upright per OS) and sits above the pointer `Listener` so it never counts as a life tap.
- Faint decorative `−`/`+` glyphs on the seated player's own left/right edges — rendered **inside the seat's `RotatedBox`** so they rotate with the seat and always match the router's per-seat sign — added as non-hittable decoration so taps fall through to the life router.
- A **floating life-change indicator** near the life number: when life changes via the tap/hold/swipe path a small signed `+N` (green `#47C266`) / `−N` (red `#E5533C`) label appears, `w800` with a soft shadow. Consecutive changes to the same player within a ~1.8s window **accumulate into one growing number** (five quick +1 taps read `+5`; a ramping hold reads a climbing `−12`; a swipe contributes ±10); after ~1.8s with no further change it fades out (`AnimatedOpacity`) and resets to nothing. It is seat-rotated (rendered inside the zone's `RotatedBox`) and display-only (`IgnorePointer`, no hit target — taps fall through to the life router). The transient per-player accumulated delta plus its reset timers live in a `lifeDeltaProvider` (`Notifier<Map<int,int>>`) in the **ui/ layer** — deliberately kept out of the pure `game/`/`touch/` layers — bumped from the `_onResult` life-dispatch path; the zone watches its player's entry and renders nothing when it is 0/expired. Commander-damage-induced life loss deliberately does not raise an indicator (it goes through its own dispatch path).
- A compact cluster of small rounded chips (radius 8) for the player's **non-zero** secondary counters (poison drop, energy bolt, experience, and any non-zero generic named counter — Treasure/Storm/Rad), just above the commander-damage strip.
- **Commander-damage map** floating over each player's zone, above the `Listener`: one **egocentric map per player** — a player's-eye view of the table — anchored in that player's **own bottom-right corner**. Small **square** tiles (40px, radius 8, dark `#1E1E1E` surface with a thin `#2C2C2C` border, 4px gaps) are laid out as a `Column`: a top **`Wrap` row of the opponent tiles** with the player's **"me" tile centered below** them, so "me" sits at the **near/bottom** side (nearest the player) and the opponents are arrayed in a row **above/in front of** them. The opponents are ordered **left-to-right exactly as this player sees them across the table**: for the current player P and each opponent O the physical vector `v = rectO.center − rectP.center` is rotated out of screen space into P's own facing frame by the negative of P's `seatQuarterTurns` (a `_rotateQuarter(offset, q)` helper rotates an offset clockwise by `q` quarter-turns; un-rotate by passing `(4 − (turns[P] & 3)) & 3`), and opponents are sorted by the resulting `.dx` ascending (P's left → right). The opponent tiles flow in a `Wrap`, so a wide row (e.g. 5 opponents at 6 players) can spill to a second row rather than overflow, with "me" always below all of them; the same rule applies for 2/3/5/6 players. The whole map sits inside the seat's rotation: for player P (zone `rectP`, quarter-turns `qP`) it is a `Positioned.fromRect(rect: rectP)` → `RotatedBox(quarterTurns: qP)` → `Align(alignment: Alignment.bottomRight)` → `Padding(10)` → the map, so it faces P and lands in P's own bottom-right corner while the transparent rest of the `Align` falls through to the `Listener` (same overlay pattern as the gear/counters/name). Because the map as a whole is already rotated, each tile passes `quarterTurns: 0` (no double rotation). Each **opponent** cell is filled with that opponent's **commander art** (`Image.network`, `BoxFit.cover`) when their `artUrl` is set, falling back to the opponent's solid **color** while loading, on error, or when no art exists — so the color never disappears — with the damage this player has taken from that commander (default 0) overlaid on a legibility scrim. Tapping a cell = **+1** commander damage from that opponent (dispatches `AdjustCommanderDamage` with `reduceLife` set from the "Commander damage life loss" setting); long-press = **−1**, clamped at 0 (disabled at 0). A cell at **21+** — lethal — flags red (a 2px red border plus a red wash), and with life-loss on the KO treatment then applies. Each cell carries `HitTestBehavior.opaque` and keeps its `ValueKey('cmdr-<playerId>-<oppId>')`, so only its own hit area is consumed; the rest of the zone still taps life. The **"me" identity holder** (`ValueKey('cmdr-me-<playerId>')`) is the **bottom, near-side tile** of the egocentric map, filled with the player's **own** commander art (or their own color) and a small "me" label; it carries no commander-damage number, and tapping it opens the player's settings sheet (rename/commander/color) rather than incrementing anything. The map is seat-relative (it reads the zone `rects` and `seatQuarterTurns`), so 2/3/5/6 each get the same bottom-right map facing their seat. All iconography stays our own Material shapes; the only imagery is the existing Scryfall-art-by-card-name mechanism. The poison/energy/experience counters stay a separate compact chip cluster near the zone's bottom edge.
- The gear icon remains in a top corner (above the `Listener`) as the second path to the per-player settings sheet; a **counters affordance** (`Icons.grid_view`, `ValueKey('counters-<id>')`) sits in the opposite top corner (also seat-rotated, above the `Listener`) and opens the counters popup.
- **Knocked-out (KO) visual** — gated by the Auto-KO setting via `_knockedOut(player, settings)` (life ≤ 0, poison ≥ 10, or, with life-loss on, any commander damage ≥ 21). When a seat is knocked out its big life number is replaced by a large skull glyph (`💀` as `Text`) over the word **KO**, sharing the seat's `RotatedBox` so it faces the player like the number did and scaled by the same `FittedBox`. The whole zone reads as out: the art background is desaturated through a `ColorFiltered` luminance saturation matrix, a dark overlay (`Color(0x8C000000)`) dims the zone under the KO mark, and the secondary counter chips are dimmed to 50%. The name pill, gear/counters affordances, and commander-damage grid (separate overlay layers) stay visible. KO triggers on 0/negative life immediately and on the other lethal thresholds; it is purely visual (never game logic).

**Counters popup** — the counters affordance opens a **seat-rotated** popup (`showDialog`, wrapped in a `RotatedBox` of the seat's `quarterTurns`) that is the fuller counter-management view. It deliberately breaks from the dark theme to match the reference: a **light rounded modal** (surface `#EDEFF2`, dark text `#1B1F24`) with an **X close** in the top corner and a segmented **Player / Counters** toggle (Counters selected by default; the Player tab is a minimal stub showing the player's color dot, name, life, and a pointer to the gear editor). The Counters tab opens with a **Status** row of three tiles for the single-holder / global statuses (see below), then the player's **active** counters (any with value > 0) as larger cyan tap-to-increment tiles with their current value, and a grid of **counter-type tiles** (Material icon + label) for the inactive types to add. Offered types: Poison / Energy / Experience (the fixed `CounterMode` fields, dispatched via `AdjustCounter`) plus generic **Treasure / Storm / Rad** (dispatched via `AdjustNamedCounter`). **Tapping** a tile is **+1** (first tap on a generic type creates its entry); **holding** an active tile is **−1** (clamped ≥ 0). Footer: "Tap to increment. Hold for additional options." Everything routes through the notifier so undo/history stay correct. Commander damage keeps its own per-zone grid and is **not** duplicated in the popup (the spec allows either; keeping it on the zone avoids per-opponent complexity in the modal).

**Table statuses (Monarch / Initiative / Day-Night)** — three game-wide statuses live on `GameState` (not on `PlayerState`): `int? monarchId`, `int? initiativeId` (both default null), and a `DayNight dayNight` enum (`none`/`day`/`night`, default `none`), all threaded through `GameState.copyWith`/`replacePlayer` so per-player mutations preserve them, and all reset by `NewGame`. Monarch and Initiative are **single-holder**: setting one to a player moves it off whoever held it, since each is one field. Three events drive them — `SetMonarch({int? playerId})`, `SetInitiative({int? playerId})` (each `copyWith`s its field; `describe` → "<player> became monarch" / "took the initiative" / "… cleared"), and `CycleDayNight()` (`none → day → night → none`; `describe` → "Day/Night: <state>"). The notifier exposes `toggleMonarch(id)` / `toggleInitiative(id)` (clear if the tapped player already holds it, else move it to them) and `cycleDayNight()`, all through `dispatch` so undo/history stay correct. In the counters popup's Status row, the **Monarch** tile (`Icons.workspace_premium`) and **Initiative** tile (`Icons.military_tech`) toggle onto the popup's player and highlight cyan while that player holds them; the **Day/Night** tile cycles the global state and shows the current step (`Icons.wb_sunny` day / `Icons.nightlight_round` night / a faded `Icons.brightness_medium` when none), highlighting when set. On the table, the Monarch holder's zone shows a crown badge (👑) and the Initiative holder's a medal badge (`Icons.military_tech`), both small discs floating near the name pill, seat-rotated to face that player, above the `Listener` but wrapped in an `IgnorePointer` (display-only, never a life tap) and shown only for the current holder. The toolbar also carries a **day/night indicator** (sun/moon, faded when none) whose tap cycles the state, so it is reachable without opening the popup.

Generic counters live in a new `Map<String,int> counters` field on `PlayerState` (default `{}`, threaded through `copyWith`), driven by a new `AdjustNamedCounter({playerId, name, delta})` event that clamps 0..999 and describes as `"<player> <name> <±delta>"`. The existing poison/energy/experience/commanderDamage state is untouched — the generic map is added alongside. **Deferred (out of this pass):** the special single-holder/global counters (Monarch, Initiative, Day/night) and KO-as-counter — they need ownership/global logic beyond the increment-style counters the popup manages.

**Seat facing** — `seatQuarterTurns` rotates each zone's content to face its seat. For **4 players** the two columns face the **side edges** rather than top/bottom: the 2×2 zone order is `[top-left, top-right, bottom-left, bottom-right]`, and the left column reads from the left side (`q1`) while the right column reads from the right side (`q3`) — i.e. `[1, 3, 1, 3]`. So the middle horizontal toolbar pairs with two players stacked facing the left and two stacked facing the right. (Note: for this codebase's `RotatedBox`, `q1` is read by a viewer on the **left** and `q3` by a viewer on the **right** — verified by render probe and consistent with the 3-player `[1, 3, 0]` = "left edge, right edge, bottom".) The per-seat tap sign and the `−`/`+` edge hints already read each zone's quarter-turns, so side-facing seats sign by the player's own left/right automatically. 2/3/5/6 layouts are unchanged.

**Toolbar** — a slim black strip (~64px) of evenly spaced white icon buttons: reset (opens Settings), a cyan-ringed player-count badge (also opens Settings), undo, dice (opens a **Dice & Coins** popup), coin flip, and history. The **dice popup** is a dark rounded modal (theme surface) titled "Dice & Coins" holding a `Wrap` of tiles — one per standard die (d4/d6/d8/d10/d12/d20) plus a **Coin** — each drawn as its **own painted shape** via a `CustomPainter` (`_DiceShapePainter`: triangle=d4, pipped square=d6, diamond=d8, kite=d10, pentagon=d12, hexagon=d20, circle=coin; accent stroke over a faint fill — no image assets) with its label. Tapping a die rolls a **uniform 1..N** from the shared `_rng` and shows the label + big number result in the modal; tapping the coin shows **Heads/Tails**. A close (X) button dismisses it. The separate toolbar coin button stays as a second path to a coin flip. For 2/4/6 players it sits **between the rows** (top row / strip / bottom row) — pairing with the top+bottom seating for 2/6 and with the left/right side-facing columns for 4 — while 3 and 5, which have no clean middle split, keep it at the bottom. The player-zone rects are computed to exclude the strip, so the top zones fill the area above it and the bottom zones below, and a toolbar touch is outside every zone (never a stray life tap).

**Settings / new-game screen** (`lib/ui/settings_screen.dart`, public `SettingsScreen`) — a black scaffold titled "Settings": a "Game Setup" header over a hairline; a single-select "Players" row (2/3/4/5/6) and "Starting life" row (20/25/30/40/60) rendered as ~44px circular chips (unselected `#2A2A2A`, selected = cyan 2px ring + cyan number); a cyan "Start game" button that dispatches `NewGame(count, life)`; and a "Gameplay" section with cyan toggle rows for "Commander damage life loss", "In-app keyboard" (a `SwitchListTile`, default on — the seat-rotated on-screen rename keyboard described above; off falls back to the OS keyboard), and "Auto-KO". These flags live in a lightweight `settingsProvider` (`GameSettings`) kept out of the event-sourced session; Auto-KO gates the zone's knocked-out visual. The "Commander damage life loss" toggle now drives `AdjustCommanderDamage.reduceLife` (an optional field, default `true`): the per-opponent commander-damage chips read the flag at dispatch, so with it **on** commander damage also subtracts life (the standard rule) and with it **off** only the per-opponent counter changes.

**History sheet** — color-coded and newest-first, projected from the event log: each life/counter change shows a dot in the player's color, the name, a counter icon (heart = life, drop = poison, bolt = energy, sparkle = experience, shield = commander damage), a rounded signed delta chip (green gain / red loss / accent counter), and "→ resulting value". An Undo button in the header updates the list live.

## Testing & CI (Docker)

All CI jobs run in containers on GitLab CI; the same containers work locally via `docker run` so dev == CI.

| Stage | Job | Container | What it does |
|---|---|---|---|
| lint | format + analyze | `ghcr.io/cirruslabs/flutter` | `dart format --set-exit-if-changed`, `flutter analyze` |
| test | unit + widget | `ghcr.io/cirruslabs/flutter` | `flutter test --coverage` — game reducers, pointer state machine (synthesized multi-pointer streams: simultaneous taps in 2 zones, 3-finger taps, ritual timing), widget tests per zone. Coverage artifact + JUnit report on the MR |
| integration | linux desktop | same image + `xvfb` | `flutter test integration_test -d linux` under xvfb — real end-to-end app boot and multi-pointer injection via `WidgetTester`. Runs on shared runners (no KVM/emulator needed) |

Android emulator testing is deliberately out of CI scope (shared runners lack KVM); integration tests run on the Linux desktop target instead, which exercises the same Dart/Flutter code paths including the pointer layer.

## Out of scope (v1)

Monarch/initiative markers, themes beyond player colors, cloud sync, iOS store release plumbing.

## Open items

- Remote hosting namespace (personal GitLab vs GitHub) — repo is local-only until decided.
- App icon / name polish.
